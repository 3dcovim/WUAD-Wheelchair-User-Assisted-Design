using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Usuario : MonoBehaviour {
	/*
	PlatformScriptWheelChair scriptA;
	void Start() {
		scriptA = this.GetComponent<PlatformScriptWheelChair> ();
	}

	void OnTriggerStay (Collider other) {
		if (other.GetComponent<hierba> () != null) {
			Debug.Log ("Hierba pisada");
			scriptA.fFactor2 = 1;
			scriptA.fFactor3 = -1;
		}
		if (other.GetComponent<arena> () != null) {
			Debug.Log ("Arena pisada");
			scriptA.fFactor2 = 0.2f;
			scriptA.fFactor3 = -5;
		}
		if (other.GetComponent<pista> () != null) {
			Debug.Log ("Pista pisada");
			scriptA.fFactor2 = 100;
			scriptA.fFactor3 = 5;
		}
		if (other.GetComponent<sueloneutro> () != null) {
			Debug.Log ("Suelo pisado");
			scriptA.fFactor2 = 10;
			scriptA.fFactor3 = 0;
		}
			


}
*/
}
